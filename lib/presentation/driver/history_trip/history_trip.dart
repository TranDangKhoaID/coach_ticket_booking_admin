import 'package:flutter/material.dart';

class HistoryTrip extends StatelessWidget {
  const HistoryTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lịch sử chuyến'),
      ),
    );
  }
}
